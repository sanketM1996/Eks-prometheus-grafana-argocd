apiVersion: v1
kind: Service
metadata:
  name: todo-backend
  namespace: three-tier

spec:
  selector:
    role: backend

  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080

  type: ClusterIP